# install-layout-navigator

This module provides an abstraction layer over the Quarkdown installation layout's `lib` directory,
generated by the root-level `build.gradle.kts`'s `installDist` and `assembleDevLib` tasks.

This directory contains resources used by the Quarkdown runtime, such as themes, fonts and JavaScript libraries.