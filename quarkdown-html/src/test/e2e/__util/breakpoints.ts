// Viewport width constants matching SCSS breakpoints in _media-queries.scss.

/** Below the sm breakpoint (800px). */
export const SM_WIDTH = 600;

/** Between sm and md breakpoints. */
export const TABLET_WIDTH = 1000;

/** Above the md breakpoint (1200px). */
export const DESKTOP_WIDTH = 1400;
